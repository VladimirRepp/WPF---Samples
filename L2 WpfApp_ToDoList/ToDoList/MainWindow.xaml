<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    
<Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                Text="Мои задачи" 
                FontSize="28" 
                FontWeight="Bold" 
                HorizontalAlignment="Center" 
                Margin="0,0,0,25"/>

        <!-- Добавление задачи -->
        <Grid Grid.Row="1" Margin="0,0,0,20">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TaskTextBox" 
                        Grid.Column="0" 
                        Height="40" 
                        FontSize="16" 
                        VerticalContentAlignment="Center"
                        Margin="0,0,10,0" 
                        KeyDown="TaskTextBox_KeyDown"/>

            <Button Grid.Column="1" 
                        Content="Добавить" 
                        Width="100" 
                        Height="40" 
                        FontSize="14" 
                        Click="AddButton_Click"/>
        </Grid>

        <!-- Список задач -->
        <ListBox x:Name="TasksListBox" Grid.Row="2" FontSize="16" BorderThickness="1">

            <ListBox.ItemTemplate>

                <DataTemplate>
                    <!-- Одна из самых важных возможностей WPF — Binding -->
                    <!-- WPF умеет связывать данные объекта со свойствами UI-элемента -->
                    <!-- Binding — это механизм автоматической связи свойства объекта с Dependency Property элемента WPF -->
                    <!-- TasksListBox - по имени обращаемся в коде -->                    
                    <CheckBox Content="{Binding Title}" 
                              IsChecked="{Binding IsCompleted}" 
                              FontSize="16" 
                              Margin="5" 
                              Checked="TaskCheckBox_Checked" 
                              Unchecked="TaskCheckBox_Unchecked"/>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Статистика -->
        <Grid Grid.Row="3" Margin="0,20,0,20">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TasksCountTextBlock" 
                        Grid.Column="0" 
                        Text="Задач: 0" 
                        FontSize="16" 
                        HorizontalAlignment="Center"/>

            <TextBlock x:Name="CompletedCountTextBlock" 
                        Grid.Column="1" 
                        Text="Выполнено: 0" 
                        FontSize="16" 
                        HorizontalAlignment="Center"/>

        </Grid>

        <!-- Кнопки управления -->
        <Grid Grid.Row="4">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>

            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                        Content="Удалить выбранную" 
                        Height="40" 
                        Margin="0,0,5,0" 
                        Click="DeleteButton_Click"/>

            <Button Grid.Column="1"         
                        Content="Очистить" 
                        Height="40" 
                        Margin="5,0,0,0" 
                        Click="ClearButton_Click"/>

        </Grid>
    </Grid>
</Window>
