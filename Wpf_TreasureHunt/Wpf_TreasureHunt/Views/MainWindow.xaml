<Window x:Class="Wpf_TreasureHunt.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:model="clr-namespace:Wpf_TreasureHunt.Models"
        Title="Охота за сокровищем"
        Width="650"
        Height="700">

    <Window.Resources>

        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary
                    Source="../Resources/Styles.xaml"/>

            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>

    </Window.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>

        <!-- Заголовок -->

        <TextBlock Grid.Row="0"
                   FontSize="26"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Text="Охота за сокровищем"/>

        <!-- Панель информации -->

        <StackPanel Grid.Row="1"
                    Margin="0 20"
                    HorizontalAlignment="Center">

            <TextBlock
                FontSize="18"
                Text="{Binding GameStatus}" />

            <TextBlock
                FontSize="16"
                Text="{Binding MovesCount,
                       StringFormat=Ходов: {0}}" />

            <Button Width="150"
                    Height="40"
                    Content="Новая игра"
                    Command="{Binding NewGameCommand}" />

            <Button Width="150"
                     Height="40"
                     Content="Правила игры"
                     Command="{Binding RoolsGameCommand}" />    

        </StackPanel>

        <!-- Игровое поле -->

        <ItemsControl Grid.Row="2"
                      ItemsSource="{Binding Cells}">

            <ItemsControl.ItemsPanel>

                <ItemsPanelTemplate>

                    <UniformGrid Rows="5"
                                 Columns="5"/>

                </ItemsPanelTemplate>

            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>

                <DataTemplate DataType="{x:Type model:Cell}">

                    <Button Content="{Binding Text}"
                            Style="{StaticResource CellButtonStyle}"
                            Command="{Binding DataContext.OpenCellCommand,
                                              RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}" />

                </DataTemplate>

            </ItemsControl.ItemTemplate>

        </ItemsControl>

    </Grid>

</Window>