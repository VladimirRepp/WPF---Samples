Реализуем в два этапа:
	сначала через C# и ValueChanged — чтобы студенты увидели привычный событийный подход;
	затем через Binding — чтобы стало очевидно, зачем WPF предоставляет этот механизм.

Будем использовать:
	Slider;
	TextBlock;
	Border;
	Grid;
	StackPanel;
	SolidColorBrush;
	события ValueChanged.